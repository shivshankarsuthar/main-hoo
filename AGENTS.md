# AGENTS.md

## Cursor Cloud specific instructions

This is a new/empty repository. As of the initial setup, it contains only a blank `README` file.

- **No languages, frameworks, or package managers** are configured yet.
- **No services** need to be started.
- **No lint, test, or build commands** exist.

When source code and dependencies are added, update this file with:
- How to install dependencies (e.g. `npm install`, `pip install -r requirements.txt`)
- How to run lint, tests, and the dev server
- Any non-obvious environment caveats
